using <%= name %>.Core.Models;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;
using System;
using System.Collections.Generic;
using System.Text;
using <%= name %>.Data.Configurations.BaseModelConfigurations;
namespace <%= name %>.Data.Configurations
{
 
    public class LogConfiguration : BaseModelConfiguration<Log, int>
        {
            public LogConfiguration(EntityTypeBuilder<Log> builder) : base(builder)
            {
                builder
                    .ToTable("Log");
            }
        }
}
